function CartPage() {
  return (
    <section className="page-section">
      <div className="container">
        <span className="section-kicker">Cart</span>
        <h1>Your selected digital products will appear here.</h1>
        <p className="lead">
          The cart route is configured for the shopping flow and can be extended
          with product totals, coupon UI, and checkout actions.
        </p>
      </div>
    </section>
  );
}

export default CartPage;
